events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        location /api/ {
            rewrite ^/api/(.*)$ /$1 break;
            proxy_pass http://api:8080;
        }

        location / {
            root /usr/share/nginx/html/app;
        }
    }
}
